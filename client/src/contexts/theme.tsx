import { Theme, createTheme } from "@mui/material";

export const theme: Theme = createTheme({
  palette: {
    background: {
      default: "#ccc",
    },
    primary: {
      main: "#333",
    },
  },
});
